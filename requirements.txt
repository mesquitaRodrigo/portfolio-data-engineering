# Data Engineering Portfolio Project - Dependencies
# Architecture: ERP (PostgreSQL) → Raw Layer → Curated Layer → DuckDB → Analytics

# Database (ERP - PostgreSQL)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Data Processing
pandas==2.1.4
pyarrow==14.0.2

# Analytics (DuckDB for Curated Layer queries)
duckdb==0.9.2

# Docker (for development)
docker==6.1.3

# Future Integration (Optional - commented out)
# Apache Airflow (Orchestration)
# apache-airflow==2.7.0

# dbt (Transformation)
# dbt-core==1.5.0
# dbt-postgres==1.5.0

# MinIO (S3-compatible storage)
# minio==7.2.0

# AWS S3 (Cloud storage)
# boto3==1.34.0

# Azure Data Lake (Cloud storage)
# azure-storage-blob==12.19.0

# Power BI (Visualization - requires Windows)
# powerbi==0.0.1
